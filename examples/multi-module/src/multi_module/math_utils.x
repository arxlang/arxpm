```
title: Arithmetic helpers
```

fn add(a: i32, b: i32) -> i32:
  ```
  title: Return the sum of two integers
  ```
  return a + b;

fn mul(a: i32, b: i32) -> i32:
  ```
  title: Return the product of two integers
  ```
  return a * b;
