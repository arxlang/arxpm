```
title: Statistics helpers exposed as local_lib.stats
```

fn sum2(a: i32, b: i32) -> i32:
  ```
  title: Return the sum of two integers
  ```
  return a + b;

fn double(x: i32) -> i32:
  ```
  title: Return twice the input
  ```
  return x + x;
