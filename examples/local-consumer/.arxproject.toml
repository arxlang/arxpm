[project]
name = "local-consumer"
version = "0.1.0"
edition = "2026"

dependencies = [
  "local_lib @ ../local_lib",
  "pyyaml",
]

[build]
src_dir = "src"
out_dir = "build"
package = "local_consumer"
mode = "app"

[toolchain]
compiler = "arx"
linker = "clang"
