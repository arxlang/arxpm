{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"ArxPM","text":"<p><code>arxpm</code> is the project and package manager for Arx workspaces.</p>"},{"location":"#scope","title":"Scope","text":"<ul> <li><code>arx</code> is compiler-only.</li> <li><code>arxpm</code> is the user-facing workflow tool.</li> <li>Python environments are backend-neutral: a project can use a project-local   venv (default), a conda environment, or the current system interpreter. <code>uv</code>   is used to install packages in all cases.</li> </ul> <p>Arx projects use <code>.arxproject.toml</code> as their project manifest. Python packaging is only for distributing <code>arxpm</code> itself.</p>"},{"location":"#compatibility","title":"Compatibility","text":"<ul> <li>Python 3.10+ is supported.</li> <li>On Python 3.10, <code>arxpm</code> uses <code>tomli</code> as a compatibility fallback for   <code>tomllib</code>.</li> </ul>"},{"location":"#architecture","title":"Architecture","text":"<ul> <li><code>src/arxpm/models.py</code>: typed manifest models.</li> <li><code>src/arxpm/manifest.py</code>: parse/render <code>.arxproject.toml</code>.</li> <li><code>src/arxpm/_toml.py</code>: TOML parser compatibility shim (<code>tomllib</code>/<code>tomli</code>).</li> <li><code>src/arxpm/environment.py</code>: environment runtime abstraction and the <code>venv</code>,   <code>conda</code>, and <code>system</code> implementations.</li> <li><code>src/arxpm/project.py</code>: <code>init</code>, <code>add</code>, <code>install</code>, <code>build</code>, <code>run</code>, <code>pack</code>,   <code>publish</code>.</li> <li><code>src/arxpm/healthcheck.py</code>: environment and manifest checks.</li> <li><code>src/arxpm/cli.py</code>: Typer CLI layer.</li> </ul>"},{"location":"#quick-cli","title":"Quick CLI","text":"<pre><code>arxpm init --name hello-arx\narxpm add http\narxpm install\narxpm install --group dev\narxpm build\narxpm compile\narxpm run\narxpm pack\narxpm publish\narxpm healthcheck\n</code></pre> <p>See Getting Started for a full setup and Environments for the supported environment strategies.</p>"},{"location":"changelog/","title":"Changelog","text":"<p>The canonical changelog is maintained in the repository root:</p> <ul> <li>CHANGELOG.md</li> </ul> <p>Releases are generated by semantic-release during CI release workflows.</p>"},{"location":"commands/","title":"Commands","text":""},{"location":"commands/#arxpm-init","title":"<code>arxpm init</code>","text":"<p>Create a new project in the target directory.</p> <pre><code>arxpm init --name hello-arx\narxpm init --directory ./my-project\narxpm init --env-kind venv --env-path ./shared-venv\narxpm init --env-kind conda --env-name myproject-env\narxpm init --env-kind system\n</code></pre> <p>Effects:</p> <ul> <li>creates <code>.arxproject.toml</code></li> <li>creates <code>src/main.x</code></li> <li>writes an explicit <code>[environment]</code> block only when <code>--env-kind</code>, <code>--env-path</code>,   or <code>--env-name</code> is provided</li> </ul> <p>See Environments for the supported strategies.</p>"},{"location":"commands/#arxpm-add","title":"<code>arxpm add</code>","text":"<p>Add a dependency entry to <code>.arxproject.toml</code>.</p> <pre><code>arxpm add http\narxpm add mylib --path ../mylib\narxpm add utils --git https://example.com/utils.git\n</code></pre>"},{"location":"commands/#arxpm-install","title":"<code>arxpm install</code>","text":"<p>Validate project metadata, prepare the configured Python environment, and install dependencies with <code>uv</code>.</p> <pre><code>arxpm install\narxpm install --directory examples\narxpm install --group dev\narxpm install --group dev,docs\narxpm install --group dev --group docs\narxpm install --dev\n</code></pre> <p>Dependency entries are installed as follows:</p> <ul> <li>registry: <code>uv pip install &lt;name&gt;</code></li> <li>path: <code>uv pip install &lt;path&gt;</code> (non-Arx paths) or pack+install+symlink flow   (Arx libraries with an <code>.arxproject.toml</code>)</li> <li>git: <code>uv pip install git+&lt;url&gt;</code></li> </ul> <p><code>--group</code> selects one or more dependency groups from <code>[dependency-groups]</code>. Repeat the flag or pass comma-separated names. <code>--dev</code> is a convenience alias for selecting the <code>dev</code> group.</p>"},{"location":"commands/#arxpm-build","title":"<code>arxpm build</code>","text":"<p>Invoke the configured Arx compiler directly.</p> <pre><code>arxpm build\narxpm build --directory examples\n</code></pre> <p>Current Arx invocation uses:</p> <pre><code>arx &lt;entry&gt; --output-file &lt;out_dir&gt;/&lt;project_name&gt;\n</code></pre>"},{"location":"commands/#arxpm-compile","title":"<code>arxpm compile</code>","text":"<p>Alias for <code>arxpm build</code>. Clearer name for anyone treating <code>build</code> as a packaging operation.</p> <pre><code>arxpm compile\narxpm compile --directory examples\n</code></pre>"},{"location":"commands/#arxpm-run","title":"<code>arxpm run</code>","text":"<p>Build and then run the produced artifact.</p> <pre><code>arxpm run\narxpm run --directory examples\n</code></pre> <p>The command shows compiler and program output directly in your terminal.</p>"},{"location":"commands/#arxpm-pack","title":"<code>arxpm pack</code>","text":"<p>Build package artifacts locally without uploading to a registry.</p> <pre><code>arxpm pack\narxpm pack --directory examples\n</code></pre>"},{"location":"commands/#arxpm-publish","title":"<code>arxpm publish</code>","text":"<p>Build and publish the current project as a Python package that bundles <code>.arxproject.toml</code> and <code>*.x</code>/<code>*.arx</code> sources. Build and upload tools (<code>build</code>, <code>twine</code>) run from the outer interpreter that is executing <code>arxpm</code>; they are never installed into your project environment.</p> <pre><code>export TWINE_USERNAME=__token__\nexport TWINE_PASSWORD=&lt;pypi-token&gt;\narxpm publish\narxpm publish --repository-url https://test.pypi.org/legacy/\narxpm publish --dry-run\n</code></pre>"},{"location":"commands/#arxpm-healthcheck","title":"<code>arxpm healthcheck</code>","text":"<p>Report environment health and manifest status.</p> <pre><code>arxpm healthcheck\n</code></pre> <p>Checks:</p> <ul> <li><code>.arxproject.toml</code> exists and parses</li> <li><code>uv</code> is on PATH</li> <li>the configured compiler (<code>toolchain.compiler</code>) is on PATH</li> <li>the declared environment is reachable</li> </ul>"},{"location":"contributing/","title":"Contributing","text":"<p>Contributions are welcome.</p>"},{"location":"contributing/#setup","title":"Setup","text":"<pre><code>git clone https://github.com/arxlang/arxpm.git\ncd arxpm\nmamba env create --file conda/dev.yaml\nconda activate arxpm\npoetry config virtualenvs.create false\npoetry install --with dev\n</code></pre>"},{"location":"contributing/#local-checks","title":"Local Checks","text":"<pre><code>makim tests.unit\nmakim tests.smoke\nmakim tests.linter\nmakim docs.build\n</code></pre>"},{"location":"contributing/#pull-requests","title":"Pull Requests","text":"<ul> <li>keep changes focused</li> <li>include tests for behavior changes</li> <li>update docs when user-facing behavior changes</li> <li>keep Python 3.10+ compatibility</li> </ul>"},{"location":"contributing/#release-model","title":"Release Model","text":"<p>This repository uses semantic-release. Follow conventional commit style in PR titles/messages.</p>"},{"location":"environments/","title":"Environments","text":"<p><code>arxpm</code> does not bundle its own Python environment. Every project declares how its Python environment should be provided, and <code>arxpm</code> uses <code>uv</code> to install packages into the interpreter you point it at.</p> <p>Three strategies are supported out of the box, each declared in <code>.arxproject.toml</code> via the <code>[environment]</code> table.</p>"},{"location":"environments/#strategy-1-venv-default","title":"Strategy 1 \u2014 <code>venv</code> (default)","text":"<p><code>arxpm</code> uses a virtual environment declared by <code>kind = \"venv\"</code>. When <code>path</code> is omitted, <code>arxpm</code> treats it as a project-local <code>.venv</code> and creates it with <code>uv venv</code> if needed.</p> <pre><code>[environment]\nkind = \"venv\"\npath = \".venv\"   # optional; defaults to \".venv\"\n</code></pre> <p>Behavior:</p> <ul> <li><code>arxpm install</code> runs <code>uv venv &lt;path&gt;</code> if the venv does not yet exist, then   <code>uv pip install --python &lt;venv&gt;/bin/python ...</code> for your dependencies.</li> <li><code>arxpm run</code> and <code>arxpm build</code> do not touch the venv; they invoke the <code>arx</code>   compiler directly from the outer PATH.</li> </ul> <p>Omit the whole <code>[environment]</code> section for the simplest projects \u2014 the defaults match what you'd write by hand.</p>"},{"location":"environments/#strategy-2-conda","title":"Strategy 2 \u2014 <code>conda</code>","text":"<p>Point <code>arxpm</code> at an existing conda (or mamba) environment by name or prefix:</p> <pre><code>[environment]\nkind = \"conda\"\nname = \"demo-env\"\n# or\n# path = \"/opt/conda/envs/demo-env\"\n</code></pre> <p>Behavior:</p> <ul> <li>If <code>path</code> is provided, <code>arxpm</code> uses <code>&lt;path&gt;/bin/python</code> directly.</li> <li>Otherwise <code>arxpm</code> invokes   <code>conda run -n &lt;name&gt; python -c \"import sys; print(sys.executable)\"</code> to locate   the interpreter, then installs via <code>uv pip install --python &lt;interp&gt;</code>.</li> <li><code>conda</code> must be on PATH when only <code>name</code> is provided.</li> </ul>"},{"location":"environments/#strategy-3-system","title":"Strategy 3 \u2014 <code>system</code>","text":"<p>Use the Python interpreter that is already running <code>arxpm</code>.</p> <pre><code>[environment]\nkind = \"system\"\n</code></pre> <p>Behavior:</p> <ul> <li><code>arxpm install</code> installs packages into the current Python environment using   <code>uv pip install --python &lt;sys.executable&gt; ...</code>.</li> <li><code>path</code> and <code>name</code> are not allowed for this mode.</li> </ul>"},{"location":"environments/#why-uv","title":"Why uv?","text":"<p><code>uv</code> is used as the package installer for every strategy. It is fast, it does not require an active venv (it accepts <code>--python &lt;interp&gt;</code>), and it is a single static binary. <code>arxpm healthcheck</code> reports whether <code>uv</code> is reachable on your PATH.</p>"},{"location":"environments/#tooling-not-managed-by-arxpm","title":"Tooling not managed by arxpm","text":"<p><code>arxpm publish</code> and <code>arxpm pack</code> use <code>python -m build</code> and <code>python -m twine</code> from the outer interpreter that is running <code>arxpm</code>. They never install build tooling into your project environment. <code>build</code> and <code>twine</code> are declared as runtime dependencies of <code>arxpm</code> itself, so they are always available wherever <code>arxpm</code> is installed.</p>"},{"location":"getting-started/","title":"Getting Started","text":"<p>This guide sets up <code>arxpm</code> for local development and smoke testing.</p>"},{"location":"getting-started/#prerequisites","title":"Prerequisites","text":"<ul> <li>Python 3.10+</li> <li><code>uv</code> (install with <code>pip install uv</code> or from the uv binary release)</li> <li>Conda or Mamba (optional; used for the repo's dev conda env)</li> </ul>"},{"location":"getting-started/#setup","title":"Setup","text":"<pre><code>git clone https://github.com/arxlang/arxpm.git\ncd arxpm\nmamba env create --file conda/dev.yaml\nconda activate arxpm\npoetry install --with dev\n</code></pre>"},{"location":"getting-started/#verify-toolchain","title":"Verify Toolchain","text":"<pre><code>python -m arxpm healthcheck --directory examples/hello-arx\n</code></pre> <p>The report should show:</p> <ul> <li><code>.arxproject.toml</code> found</li> <li>manifest parses</li> <li><code>uv</code> is available on PATH</li> <li>the configured compiler (<code>arx</code>) is on PATH</li> <li>the environment is configurable (defaults to a project <code>.venv</code>)</li> </ul>"},{"location":"getting-started/#run-examples","title":"Run Examples","text":"<p>The <code>examples/</code> directory ships several projects:</p> <ul> <li><code>examples/hello-arx/</code> \u2014 single-file project that prints a greeting.</li> <li><code>examples/multi-module/</code> \u2014 multi-file project where <code>main.x</code> imports helpers   from sibling modules (<code>math_utils.x</code>, <code>string_utils.x</code>). See   Multi-file Projects for the layout.</li> <li><code>examples/local_lib/</code> + <code>examples/local-consumer/</code> \u2014 path-dependency workflow   where one Arx project depends on another. See   Local Packages.</li> </ul> <pre><code>python -m arxpm install --directory examples/hello-arx\npython -m arxpm build --directory examples/hello-arx\npython -m arxpm run --directory examples/hello-arx\n\npython -m arxpm install --directory examples/multi-module\npython -m arxpm run --directory examples/multi-module\n</code></pre> <p>The first <code>install</code> creates <code>examples/hello-arx/.venv</code> via <code>uv venv</code>. The multi-module run prints:</p> <pre><code>Hello, Arx!\n5\n</code></pre>"},{"location":"getting-started/#publish-package","title":"Publish Package","text":"<p>Set Twine credentials for your package index (PyPI example):</p> <pre><code>export TWINE_USERNAME=__token__\nexport TWINE_PASSWORD=&lt;pypi-token&gt;\npython -m arxpm publish --directory examples/hello-arx\n</code></pre> <p><code>publish</code> uses <code>python -m build</code> and <code>python -m twine</code> from the outer interpreter running <code>arxpm</code>; it never installs build tooling into the project environment.</p>"},{"location":"getting-started/#local-quality-gates","title":"Local Quality Gates","text":"<pre><code>makim tests.unit\nmakim tests.smoke\nmakim tests.linter\nmakim docs.build\n</code></pre>"},{"location":"local-packages/","title":"Local Packages","text":"<p>A library and its consumer can live side by side on disk and be wired together with a path dependency. <code>arxpm install</code> handles the packaging / install / link step so the Arx compiler finds the library's modules at build time.</p>"},{"location":"local-packages/#supported-layout","title":"Supported layout","text":"<p>Arx projects default to a <code>src/</code> source root. The library's <code>.x</code> sources sit under <code>src/</code>, the consumer's sources sit under its own <code>src/</code>, and each side declares <code>src_dir = \"src\"</code> in <code>[build]</code>:</p> <pre><code>workspace/\n\u251c\u2500\u2500 local_lib/\n\u2502   \u251c\u2500\u2500 .arxproject.toml\n\u2502   \u2514\u2500\u2500 src/\n\u2502       \u251c\u2500\u2500 local_lib.x\n\u2502       \u2514\u2500\u2500 stats.x\n\u2514\u2500\u2500 local-consumer/\n    \u251c\u2500\u2500 .arxproject.toml\n    \u2514\u2500\u2500 src/\n        \u2514\u2500\u2500 main.x\n</code></pre> <p>The library's <code>project.name</code> becomes the Arx module name used by consumers. <code>arxpm pack</code> strips the <code>src_dir</code> prefix when bundling, so the wheel lays the sources out flat under <code>&lt;package&gt;/</code>. That means <code>import sum2 from local_lib.stats</code> resolves to the <code>stats.x</code> file inside the package directory named <code>local_lib/</code> (not <code>local_lib/src/stats.x</code>).</p>"},{"location":"local-packages/#customizing-the-source-directory","title":"Customizing the source directory","text":"<p><code>src_dir</code> defaults to <code>\"src\"</code> but you can override it per project \u2014 for example, a library that keeps its sources at the project root would declare <code>src_dir = \".\"</code>, and one that uses a <code>lib/</code> root would declare <code>src_dir = \"lib\"</code>. <code>entry</code> is always interpreted relative to <code>src_dir</code>, so the entry path passed to the compiler is <code>&lt;src_dir&gt;/&lt;entry&gt;</code>.</p>"},{"location":"local-packages/#declaring-the-dependency","title":"Declaring the dependency","text":"<p><code>local-consumer/.arxproject.toml</code>:</p> <pre><code>[project]\nname = \"local-consumer\"\nversion = \"0.1.0\"\nedition = \"2026\"\n\ndependencies = [\n  \"local_lib @ ../local_lib\",\n]\n\n[build]\nsrc_dir = \"src\"\nentry = \"main.x\"\nout_dir = \"build\"\n\n[toolchain]\ncompiler = \"arx\"\nlinker = \"clang\"\n</code></pre> <p>The name on the left of <code>@</code> must match the library's Arx module name (derived from its <code>project.name</code>, normalized to a Python identifier). <code>arxpm install</code> rejects mismatched names rather than producing a silent failure at build time.</p>"},{"location":"local-packages/#what-arxpm-install-does-for-an-arx-path-dependency","title":"What <code>arxpm install</code> does for an Arx path dependency","text":"<p>For each <code>project.dependencies</code> entry of the form <code>&lt;name&gt; @ &lt;path&gt;</code> where <code>&lt;path&gt;</code> points at a directory containing <code>.arxproject.toml</code>, <code>arxpm install</code>:</p> <ol> <li>Packs the library (runs <code>arxpm pack</code> on that path) to produce a wheel that    bundles every <code>.x</code> / <code>.arx</code> source under the library's <code>src_dir</code>, flattened    to the package root.</li> <li>Installs the wheel into the consumer's Python environment via    <code>uv pip install --python &lt;interp&gt; --force-reinstall --no-deps &lt;wheel&gt;</code>, so    the library lands at <code>&lt;env&gt;/lib/pythonX/site-packages/&lt;module_name&gt;/</code>.</li> <li>Creates a symlink <code>&lt;consumer&gt;/&lt;module_name&gt;</code> pointing at the installed    directory. The Arx compiler's resolver walks the ancestors of the entry file    looking for <code>&lt;module_name&gt;/&lt;sub&gt;.x</code>, so the symlink at the consumer's project    root is what lets the import succeed.</li> </ol> <p>Commit the <code>.arxproject.toml</code> entry; ignore the generated symlink:</p> <pre><code># local-consumer/.gitignore\n/local_lib\n</code></pre>"},{"location":"local-packages/#example","title":"Example","text":"<p>The <code>examples/</code> directory ships a working pair:</p> <ul> <li><code>examples/local_lib/</code></li> <li><code>examples/local-consumer/</code></li> </ul> <p>Copy them so they sit next to each other, then from the consumer:</p> <pre><code>cd local-consumer\narxpm install\narxpm build\narxpm run\n</code></pre> <p>Expected stdout: <code>5</code>.</p>"},{"location":"local-packages/#current-limits","title":"Current limits","text":"<ul> <li>Compiler resolution is local, not site-packages. The Arx compiler's   <code>FileImportResolver</code> only searches CWD and ancestors of the input files \u2014 it   does not read from <code>site-packages</code> directly. The symlink step is what bridges   the installed wheel back into the consumer's ancestor tree.</li> <li>Path deps only; no registry or git. <code>&lt;name&gt; @ git+...</code> dependencies still   go straight through <code>uv pip install</code> and bring no cross-compile support today   \u2014 only <code>&lt;name&gt; @ &lt;path&gt;</code> entries pointing at an Arx project trigger the   pack/install/link flow.</li> <li>No version solving or editable installs. Every <code>arxpm install</code> re-packs   the library and reinstalls the wheel. For iterative library development, edit   sources and re-run <code>arxpm install</code> in the consumer.</li> <li>No circular dependencies. A and B cannot depend on each other.</li> </ul>"},{"location":"manifest/","title":"Manifest","text":"<p>Arx projects are described by <code>.arxproject.toml</code>.</p>"},{"location":"manifest/#minimal-layout","title":"Minimal Layout","text":"<pre><code>[project]\nname = \"hello-arx\"\nversion = \"0.1.0\"\nedition = \"2026\"\ndependencies = []\n\n[build]\nsrc_dir = \"src\"\nentry = \"main.x\"\nout_dir = \"build\"\n\n[toolchain]\ncompiler = \"arx\"\nlinker = \"clang\"\n</code></pre>"},{"location":"manifest/#source-layout","title":"Source Layout","text":"<p><code>build.src_dir</code> names the directory where your <code>.x</code> sources live, relative to the project root. It defaults to <code>\"src\"</code> (the recommended layout). <code>build.entry</code> is always interpreted relative to <code>src_dir</code>, so with the default layout a file at <code>src/main.x</code> is spelled <code>entry = \"main.x\"</code>.</p> <p>Override <code>src_dir</code> when your project uses a different convention \u2014 for example, <code>src_dir = \".\"</code> to keep sources at the project root, or <code>src_dir = \"lib\"</code> to use a <code>lib/</code> folder. Cross-project tooling (<code>arxpm build</code>, <code>arxpm pack</code>) uses <code>&lt;src_dir&gt;/&lt;entry&gt;</code> internally and strips <code>src_dir</code> when bundling for publication, so published modules always appear flat under their package name.</p>"},{"location":"manifest/#runtime-dependencies","title":"Runtime Dependencies","text":"<p>Runtime dependencies live in the <code>project.dependencies</code> array and use PEP 508-style strings. <code>arxpm</code> supports three shapes:</p> <ol> <li>Registry (bare name):</li> </ol> <pre><code>dependencies = [\n  \"pyyaml\",\n]\n</code></pre> <ol> <li>Local path:</li> </ol> <pre><code>dependencies = [\n  \"mylib @ ../mylib\",\n]\n</code></pre> <ol> <li>Git:</li> </ol> <pre><code>dependencies = [\n  \"utils @ git+https://example.com/utils.git\",\n]\n</code></pre> <p>Version solving is intentionally out of scope in v0. During <code>arxpm install</code>, registry dependencies are installed with <code>uv pip install &lt;name&gt;</code>, path dependencies with <code>uv pip install &lt;path&gt;</code>, and git dependencies with <code>uv pip install git+&lt;url&gt;</code>.</p>"},{"location":"manifest/#dependency-groups","title":"Dependency Groups","text":"<p>Non-runtime workflow dependencies such as test, lint, and docs tools live in the top-level <code>[dependency-groups]</code> table.</p> <pre><code>[dependency-groups]\ndev = [\n  \"pytest\",\n  \"ruff\",\n]\ndocs = [\n  \"mkdocs\",\n]\n</code></pre> <p>Groups can include other groups using <code>include-group</code> entries:</p> <pre><code>[dependency-groups]\nlint = [\n  \"ruff\",\n  \"mypy\",\n]\ndev = [\n  { include-group = \"lint\" },\n  \"pytest\",\n]\n</code></pre> <p><code>arxpm install --group dev</code> resolves the selected group plus any nested includes. Group names are matched using the same normalized form as <code>arx.settings</code>, so names like <code>Dev_Test</code>, <code>dev-test</code>, and <code>dev.test</code> refer to the same logical group.</p>"},{"location":"manifest/#environment","title":"Environment","text":"<p>The <code>[environment]</code> table controls how <code>arxpm install</code> obtains a Python environment for the project. It is optional \u2014 when absent, <code>arxpm</code> behaves as if <code>kind = \"venv\"</code> with <code>path = \".venv\"</code> had been declared.</p> <pre><code>[environment]\nkind = \"venv\"               # venv | conda | system\npath = \".venv\"              # optional for venv and conda\nname = \"my-conda-env\"       # optional for conda when path is omitted\n</code></pre> <p>Validation rules:</p> <ul> <li><code>venv</code>: <code>name</code> is not allowed. <code>path</code> defaults to <code>\".venv\"</code>.</li> <li><code>conda</code>: at least one of <code>name</code> or <code>path</code> is required.</li> <li><code>system</code>: neither <code>path</code> nor <code>name</code> is allowed.</li> <li>Any other <code>kind</code> value is rejected with a manifest error.</li> </ul> <p>See Environments for how each strategy behaves at runtime.</p>"},{"location":"multi-file-projects/","title":"Multi-file Projects","text":"<p>An Arx project can be composed of several <code>.x</code> files that refer to each other via <code>import</code> statements. Only the entry module is named in <code>.arxproject.toml</code> \u2014 the compiler discovers sibling modules on the filesystem at build time.</p>"},{"location":"multi-file-projects/#layout","title":"Layout","text":"<pre><code>multi-module/\n\u251c\u2500\u2500 .arxproject.toml\n\u2514\u2500\u2500 src/\n    \u251c\u2500\u2500 main.x\n    \u251c\u2500\u2500 math_utils.x\n    \u2514\u2500\u2500 string_utils.x\n</code></pre> <p><code>.arxproject.toml</code>:</p> <pre><code>[project]\nname = \"multi-module\"\nversion = \"0.1.0\"\nedition = \"2026\"\n\n[build]\nentry = \"src/main.x\"\nout_dir = \"build\"\n\n[toolchain]\ncompiler = \"arx\"\nlinker = \"clang\"\n</code></pre> <p>The manifest lists only <code>src/main.x</code>. <code>arxpm build</code> invokes the compiler as <code>arx src/main.x --output-file build/multi-module</code>; the compiler resolves <code>import</code> statements against sibling files.</p>"},{"location":"multi-file-projects/#import-syntaxes","title":"Import syntaxes","text":"<p>The following forms are accepted in any <code>.x</code> file:</p> <pre><code>import std.math\nimport std.math as math\n\nimport sin from std.math\nimport sin as sine from std.math\n\nimport (sin, cos, tan as tangent) from std.math\n\nimport (\n  sin,\n  cos,\n  tan as tangent,\n) from std.math\n</code></pre> <p>Dotted names resolve to directories: <code>import std.math</code> looks for <code>std/math.x</code> under the entry file's directory and its ancestors.</p>"},{"location":"multi-file-projects/#calling-imported-functions","title":"Calling imported functions","text":"<p><code>src/math_utils.x</code>:</p> <pre><code>fn add(a: i32, b: i32) -&gt; i32:\n  return a + b;\n</code></pre> <p><code>src/string_utils.x</code>:</p> <pre><code>fn greet(name: string) -&gt; string:\n  return \"Hello, \" + name + \"!\";\n</code></pre> <p><code>src/main.x</code>:</p> <pre><code>import add from math_utils\nimport greet from string_utils\n\nfn main() -&gt; i32:\n  print(greet(\"Arx\"));\n  print(add(2, 3));\n  return 0;\n</code></pre> <p>Running this project with <code>arxpm run</code> prints:</p> <pre><code>Hello, Arx!\n5\n</code></pre> <p>A working copy of this project lives at <code>examples/multi-module/</code>.</p>"},{"location":"multi-file-projects/#packaging","title":"Packaging","text":"<p><code>arxpm pack</code> and <code>arxpm publish</code> bundle every <code>*.x</code> / <code>*.arx</code> file under the project root (excluding <code>build/</code>, <code>dist/</code>, virtualenvs, and caches). Multi-file projects publish without additional configuration.</p>"}]}