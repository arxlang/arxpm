[project]
name = "local-consumer"
version = "0.1.0"
edition = "2026"

[build]
entry = "src/main.x"
out_dir = "build"

[dependencies]
local_lib = { path = "../local_lib" }

[toolchain]
compiler = "arx"
linker = "clang"
