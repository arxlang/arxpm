[project]
name = "multi-module"
version = "0.1.0"
edition = "2026"

dependencies = []

[build]
src_dir = "src"
out_dir = "build"
package = "multi_module"
mode = "app"

[toolchain]
compiler = "arx"
linker = "clang"
