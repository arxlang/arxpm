{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"ArxPM","text":"<p><code>arxpm</code> is the project and package manager for Arx workspaces.</p>"},{"location":"#scope","title":"Scope","text":"<ul> <li><code>arx</code> is compiler-only.</li> <li><code>arxpm</code> resolves project layout, validates package roots, and chooses default   build targets.</li> <li>Python environments are backend-neutral: a project can use a project-local   venv (default), a conda environment, or the current system interpreter. <code>uv</code>   is used to install packages in all cases.</li> </ul>"},{"location":"#architecture","title":"Architecture","text":"<ul> <li><code>src/arxpm/models.py</code>: typed manifest models.</li> <li><code>src/arxpm/manifest.py</code>: parse/render <code>.arxproject.toml</code>.</li> <li><code>src/arxpm/layout.py</code>: resolve effective <code>src_dir</code>, <code>package</code>, and <code>mode</code>.</li> <li><code>src/arxpm/environment.py</code>: environment runtime abstraction and the <code>venv</code>,   <code>conda</code>, and <code>system</code> implementations.</li> <li><code>src/arxpm/credentials.py</code>: keyring-backed publish credential storage.</li> <li><code>src/arxpm/project.py</code>: <code>init</code>, <code>add</code>, <code>install</code>, <code>build</code>, <code>run</code>, <code>pack</code>,   <code>publish</code>.</li> <li><code>src/arxpm/healthcheck.py</code>: manifest, layout, environment, and toolchain   checks.</li> <li><code>src/arxpm/cli.py</code>: Typer CLI layer.</li> </ul>"},{"location":"#quick-cli","title":"Quick CLI","text":"<pre><code>arxpm init --name hello-arx\narxpm add http\narxpm install\narxpm build\narxpm run\narxpm doctor\n</code></pre>"},{"location":"changelog/","title":"Changelog","text":"<p>The canonical changelog is maintained in the repository root:</p> <ul> <li>CHANGELOG.md</li> </ul> <p>Releases are generated by semantic-release during CI release workflows.</p>"},{"location":"commands/","title":"Commands","text":""},{"location":"commands/#arxpm-init","title":"<code>arxpm init</code>","text":"<p>Create a new app project in the target directory.</p> <pre><code>arxpm init --name hello-arx\narxpm init --directory ./my-project\narxpm init --env-kind venv --env-path ./shared-venv\narxpm init --env-kind conda --env-name myproject-env\narxpm init --env-kind system\n</code></pre> <p>Effects:</p> <ul> <li>creates <code>.arxproject.toml</code></li> <li>creates <code>src/&lt;package&gt;/__init__.x</code></li> <li>creates <code>src/&lt;package&gt;/main.x</code></li> <li>writes <code>build.mode = \"app\"</code></li> <li>writes <code>build.package</code> when <code>project.name</code> is not a valid package identifier</li> <li>writes an explicit <code>[environment]</code> block only when <code>--env-kind</code>, <code>--env-path</code>,   or <code>--env-name</code> is provided</li> </ul>"},{"location":"commands/#arxpm-config","title":"<code>arxpm config</code>","text":"<p>Configure user-level arxpm settings. Publish tokens are stored in the system keyring only; arxpm does not write tokens to project files or plaintext config files.</p> <pre><code>arxpm config pypi-token.pypi\narxpm config pypi-token.testpypi\narxpm config --unset pypi-token.pypi\n</code></pre> <p>The token value is entered through a hidden prompt. If no supported keyring is available, use <code>ARXPM_PUBLISH_TOKEN</code> for a single publish or configure a keyring backend.</p>"},{"location":"commands/#arxpm-add","title":"<code>arxpm add</code>","text":"<p>Add a dependency entry to <code>.arxproject.toml</code>.</p> <pre><code>arxpm add http\narxpm add mylib --path ../mylib\narxpm add utils --git https://example.com/utils.git\n</code></pre>"},{"location":"commands/#arxpm-install","title":"<code>arxpm install</code>","text":"<p>Validate project metadata, prepare the configured Python environment, and install dependencies with <code>uv</code>.</p> <pre><code>arxpm install\narxpm install --directory examples\narxpm install --group dev\n</code></pre>"},{"location":"commands/#arxpm-build","title":"<code>arxpm build</code>","text":"<p>Resolve the effective layout, validate it, choose the default target file, and invoke the configured Arx compiler directly.</p> <pre><code>arxpm build\narxpm build --directory examples\n</code></pre> <p>Arx invocation uses one of these targets:</p> <ul> <li>app: <code>arx &lt;src_dir&gt;/&lt;package&gt;/main.x --output-file &lt;out_dir&gt;/&lt;package&gt;</code></li> <li>lib: <code>arx &lt;src_dir&gt;/&lt;package&gt;/__init__.x --output-file &lt;out_dir&gt;/&lt;package&gt;</code></li> </ul>"},{"location":"commands/#arxpm-compile","title":"<code>arxpm compile</code>","text":"<p>Alias for <code>arxpm build</code>.</p>"},{"location":"commands/#arxpm-run","title":"<code>arxpm run</code>","text":"<p>Build and then run the produced artifact.</p> <pre><code>arxpm run\narxpm run --directory examples\n</code></pre> <p><code>arxpm run</code> is only valid for app projects.</p>"},{"location":"commands/#arxpm-pack","title":"<code>arxpm pack</code>","text":"<p>Build package artifacts locally without uploading to a registry. The command produces both a source distribution (<code>.tar.gz</code>) and a wheel (<code>.whl</code>) so source files remain available for debugging and import resolution.</p>"},{"location":"commands/#arxpm-publish","title":"<code>arxpm publish</code>","text":"<p>Build and publish the current project as a Python package that bundles <code>.arxproject.toml</code> and <code>*.x</code>/<code>*.arx</code> sources in both source distribution and wheel artifacts. The generated package metadata also includes <code>project.dependencies</code>, so Python installers can resolve transitive package dependencies.</p> <p>The default repository is the official PyPI upload endpoint: <code>https://upload.pypi.org/legacy/</code>. Override it with <code>--repository-url</code> or <code>ARXPM_PUBLISH_REPOSITORY_URL</code>.</p> <p>Use <code>ARXPM_PUBLISH_TOKEN</code> for PyPI or TestPyPI API tokens:</p> <pre><code>ARXPM_PUBLISH_TOKEN=\"pypi-...\" arxpm publish\nARXPM_PUBLISH_REPOSITORY_URL=\"https://test.pypi.org/legacy/\" \\\n  ARXPM_PUBLISH_TOKEN=\"pypi-...\" arxpm publish\n</code></pre> <p>To store a token safely for repeated local publishes, use:</p> <pre><code>arxpm config pypi-token.pypi\narxpm config pypi-token.testpypi\n</code></pre> <p><code>pypi-token.pypi</code> is used for the default PyPI upload URL. <code>pypi-token.testpypi</code> is used when publishing to <code>https://test.pypi.org/legacy/</code>.</p> <p>For repositories that use basic authentication, set <code>ARXPM_PUBLISH_USERNAME</code> and <code>ARXPM_PUBLISH_PASSWORD</code>.</p>"},{"location":"commands/#arxpm-healthcheck","title":"<code>arxpm healthcheck</code>","text":"<p>Report manifest, layout, environment, and toolchain health.</p> <pre><code>arxpm healthcheck\n</code></pre>"},{"location":"commands/#arxpm-doctor","title":"<code>arxpm doctor</code>","text":"<p>Alias for <code>arxpm healthcheck</code> with the same checks and exit behavior.</p> <pre><code>arxpm doctor\n</code></pre>"},{"location":"contributing/","title":"Contributing","text":"<p>Contributions are welcome.</p>"},{"location":"contributing/#setup","title":"Setup","text":"<pre><code>git clone https://github.com/arxlang/arxpm.git\ncd arxpm\nmamba env create --file conda/dev.yaml\nconda activate arxpm\npoetry config virtualenvs.create false\npoetry install --with dev\n</code></pre>"},{"location":"contributing/#local-checks","title":"Local Checks","text":"<pre><code>makim tests.unit\nmakim tests.smoke\nmakim tests.linter\nmakim docs.build\n</code></pre> <p><code>makim tests.smoke</code> installs and builds every example, but only runs examples whose resolved <code>build.mode</code> is <code>\"app\"</code>. Library examples are expected to stop after a successful build.</p>"},{"location":"contributing/#pull-requests","title":"Pull Requests","text":"<ul> <li>keep changes focused</li> <li>include tests for behavior changes</li> <li>update docs when user-facing behavior changes</li> <li>keep Python 3.10+ compatibility</li> </ul>"},{"location":"contributing/#release-model","title":"Release Model","text":"<p>This repository uses semantic-release. Follow conventional commit style in PR titles/messages.</p>"},{"location":"environments/","title":"Environments","text":"<p><code>arxpm</code> does not bundle its own Python environment. Every project declares how its Python environment should be provided, and <code>arxpm</code> uses <code>uv</code> to install packages into the interpreter you point it at.</p> <p>Three strategies are supported out of the box, each declared in <code>.arxproject.toml</code> via the <code>[environment]</code> table.</p>"},{"location":"environments/#strategy-1-venv-default","title":"Strategy 1 \u2014 <code>venv</code> (default)","text":"<p><code>arxpm</code> uses a virtual environment declared by <code>kind = \"venv\"</code>. When <code>path</code> is omitted, <code>arxpm</code> treats it as a project-local <code>.venv</code> and creates it with <code>uv venv</code> if needed.</p> <pre><code>[environment]\nkind = \"venv\"\npath = \".venv\"   # optional; defaults to \".venv\"\n</code></pre> <p>Behavior:</p> <ul> <li><code>arxpm install</code> runs <code>uv venv &lt;path&gt;</code> if the venv does not yet exist, then   <code>uv pip install --python &lt;venv&gt;/bin/python ...</code> for your dependencies.</li> <li><code>arxpm run</code> and <code>arxpm build</code> do not touch the venv; they invoke the <code>arx</code>   compiler directly from the outer PATH.</li> </ul> <p>Omit the whole <code>[environment]</code> section for the simplest projects \u2014 the defaults match what you'd write by hand.</p>"},{"location":"environments/#strategy-2-conda","title":"Strategy 2 \u2014 <code>conda</code>","text":"<p>Point <code>arxpm</code> at an existing conda (or mamba) environment by name or prefix:</p> <pre><code>[environment]\nkind = \"conda\"\nname = \"demo-env\"\n# or\n# path = \"/opt/conda/envs/demo-env\"\n</code></pre> <p>Behavior:</p> <ul> <li>If <code>path</code> is provided, <code>arxpm</code> uses <code>&lt;path&gt;/bin/python</code> directly.</li> <li>Otherwise <code>arxpm</code> invokes   <code>conda run -n &lt;name&gt; python -c \"import sys; print(sys.executable)\"</code> to locate   the interpreter, then installs via <code>uv pip install --python &lt;interp&gt;</code>.</li> <li><code>conda</code> must be on PATH when only <code>name</code> is provided.</li> </ul>"},{"location":"environments/#strategy-3-system","title":"Strategy 3 \u2014 <code>system</code>","text":"<p>Use the Python interpreter that is already running <code>arxpm</code>.</p> <pre><code>[environment]\nkind = \"system\"\n</code></pre> <p>Behavior:</p> <ul> <li><code>arxpm install</code> installs packages into the current Python environment using   <code>uv pip install --python &lt;sys.executable&gt; ...</code>.</li> <li><code>path</code> and <code>name</code> are not allowed for this mode.</li> </ul>"},{"location":"environments/#why-uv","title":"Why uv?","text":"<p><code>uv</code> is used as the package installer for every strategy. It is fast, it does not require an active venv (it accepts <code>--python &lt;interp&gt;</code>), and it is a single static binary. <code>arxpm doctor</code> reports whether <code>uv</code> is reachable on your PATH.</p>"},{"location":"environments/#tooling-not-managed-by-arxpm","title":"Tooling not managed by arxpm","text":"<p><code>arxpm pack</code> and <code>arxpm publish</code> use Python packaging tooling from the outer interpreter that is running <code>arxpm</code>. They never install build or upload tooling into your project environment. These tools are declared as runtime dependencies of <code>arxpm</code> itself, so they are always available wherever <code>arxpm</code> is installed.</p> <p>Configure publish credentials with the <code>ARXPM_PUBLISH_*</code> environment variables documented in the command reference. <code>ARXPM_PUBLISH_REPOSITORY_URL</code> defaults to the official PyPI upload endpoint when unset. For repeated local publishes, <code>arxpm config pypi-token.&lt;repository&gt;</code> stores tokens in the system keyring only and does not fall back to plaintext files.</p>"},{"location":"getting-started/","title":"Getting Started","text":"<p>This guide sets up <code>arxpm</code> for local development and smoke testing.</p>"},{"location":"getting-started/#prerequisites","title":"Prerequisites","text":"<ul> <li>Python 3.10+</li> <li><code>uv</code> (install with <code>pip install uv</code> or from the uv binary release)</li> <li>Conda or Mamba (optional; used for the repo's dev conda env)</li> </ul>"},{"location":"getting-started/#setup","title":"Setup","text":"<pre><code>git clone https://github.com/arxlang/arxpm.git\ncd arxpm\nmamba env create --file conda/dev.yaml\nconda activate arxpm\npoetry install --with dev\n</code></pre>"},{"location":"getting-started/#verify-toolchain","title":"Verify Toolchain","text":"<pre><code>python -m arxpm doctor --directory examples/hello-arx\n</code></pre> <p>The report should show:</p> <ul> <li><code>.arxproject.toml</code> found</li> <li>manifest parses</li> <li>package name is valid</li> <li>source root and package root exist</li> <li><code>__init__.x</code> exists</li> <li><code>main.x</code> matches the resolved mode</li> <li><code>uv</code> is available on PATH</li> <li>the configured compiler (<code>arx</code>) is on PATH</li> <li>the environment is configurable (defaults to a project <code>.venv</code>)</li> </ul>"},{"location":"getting-started/#run-examples","title":"Run Examples","text":"<p>The <code>examples/</code> directory ships several projects:</p> <ul> <li><code>examples/hello-arx/</code> \u2014 app project rooted at <code>src/hello_arx/</code>.</li> <li><code>examples/multi-module/</code> \u2014 multi-file app project rooted at   <code>src/multi_module/</code>.</li> <li><code>examples/local_lib/</code> + <code>examples/local-consumer/</code> \u2014 path-dependency workflow   with a library package and an app package.</li> </ul>"},{"location":"local-packages/","title":"Local Packages","text":"<p>A library and its consumer can live side by side on disk and be wired together with a path dependency. <code>arxpm install</code> handles the packaging / install / link step so the Arx compiler finds the library's modules at build time.</p> <p>Published Arx packages use the same runtime shape: wheels and source distributions include <code>.arxproject.toml</code> plus <code>*.x</code> / <code>*.arx</code> source files. After <code>uv</code> installs registry, Git, or local wheel dependencies, <code>arxpm install</code> links any installed Arx package sources in the dependency closure into the consumer project root.</p>"},{"location":"local-packages/#supported-layout","title":"Supported layout","text":"<pre><code>workspace/\n\u251c\u2500\u2500 local_lib/\n\u2502   \u251c\u2500\u2500 .arxproject.toml\n\u2502   \u2514\u2500\u2500 src/\n\u2502       \u2514\u2500\u2500 local_lib/\n\u2502           \u251c\u2500\u2500 __init__.x\n\u2502           \u2514\u2500\u2500 stats.x\n\u2514\u2500\u2500 local-consumer/\n    \u251c\u2500\u2500 .arxproject.toml\n    \u2514\u2500\u2500 src/\n        \u2514\u2500\u2500 local_consumer/\n            \u251c\u2500\u2500 __init__.x\n            \u2514\u2500\u2500 main.x\n</code></pre> <p>Library manifest:</p> <pre><code>[project]\nname = \"local_lib\"\nversion = \"0.1.0\"\n\n[build]\nmode = \"lib\"\n</code></pre> <p>Consumer manifest:</p> <pre><code>[project]\nname = \"local-consumer\"\nversion = \"0.1.0\"\n\ndependencies = [\n  \"local_lib @ ../local_lib\",\n]\n\n[build]\npackage = \"local_consumer\"\nmode = \"app\"\n</code></pre> <p>The dependency name on the left of <code>@</code> must match the library's resolved Arx package name. <code>arxpm install</code> rejects mismatches instead of guessing.</p>"},{"location":"local-packages/#what-arxpm-install-does","title":"What <code>arxpm install</code> does","text":"<p>For each <code>project.dependencies</code> entry of the form <code>&lt;name&gt; @ &lt;path&gt;</code> where <code>&lt;path&gt;</code> points at a directory containing <code>.arxproject.toml</code>, <code>arxpm install</code>:</p> <ol> <li>Validates the library layout.</li> <li>Packs the library to produce a wheel that bundles its <code>.x</code> / <code>.arx</code> sources.</li> <li>Installs the wheel into the consumer's Python environment with <code>uv</code>.</li> <li>Creates a symlink <code>&lt;consumer&gt;/&lt;package&gt;</code> pointing at the installed package    directory so Arx import resolution can find it locally.</li> </ol> <p>For registry, Git, or wheel dependencies, <code>arxpm install</code> first lets <code>uv</code> install the requested packages and their metadata-declared transitive dependencies. It then inspects the installed dependency closure for bundled <code>.arxproject.toml</code> files and links each discovered Arx source root as <code>&lt;consumer&gt;/&lt;package&gt;</code>. Normal Python-only dependencies are left untouched.</p> <p>For packaging, <code>arxpm pack</code> and <code>arxpm publish</code> render dependencies into the generated <code>pyproject.toml</code>. Registry dependencies remain plain package names, Git dependencies are rendered as <code>name @ git+...</code>, and path dependencies are rendered by name so published packages can resolve them from the target package index.</p>"},{"location":"manifest/","title":"Manifest","text":"<p>Arx projects are described by <code>.arxproject.toml</code>.</p>"},{"location":"manifest/#build-contract","title":"Build contract","text":"<pre><code>[project]\nname = \"hello-arx\"\nversion = \"0.1.0\"\nedition = \"2026\"\ndependencies = []\n\n[build]\nsrc_dir = \"src\"\nout_dir = \"build\"\npackage = \"hello_arx\"\nmode = \"app\"\n\n[toolchain]\ncompiler = \"arx\"\nlinker = \"clang\"\n</code></pre> <p><code>[build]</code> supports:</p> <ul> <li><code>src_dir</code>: optional, defaults to <code>\"src\"</code></li> <li><code>out_dir</code>: optional, defaults to <code>\"build\"</code></li> <li><code>package</code>: optional, defaults to <code>project.name</code></li> <li><code>mode</code>: optional, <code>\"lib\"</code> or <code>\"app\"</code></li> </ul> <p><code>[build].entry</code> was removed. <code>arxpm</code> rejects manifests that still declare it.</p>"},{"location":"manifest/#source-layout","title":"Source layout","text":"<p><code>arxpm</code> resolves the effective layout from the project root:</p> <ul> <li><code>source_root = &lt;project&gt;/&lt;src_dir&gt;</code></li> <li><code>package_root = &lt;source_root&gt;/&lt;package&gt;</code></li> <li><code>init_file = &lt;package_root&gt;/__init__.x</code></li> <li><code>main_file = &lt;package_root&gt;/main.x</code></li> </ul> <p><code>__init__.x</code> is always required.</p> <p>If <code>mode</code> is omitted, <code>arxpm</code> infers it from the filesystem:</p> <ul> <li>if <code>main.x</code> exists, the project is treated as an app</li> <li>otherwise, the project is treated as a library</li> </ul> <p>Validation rules:</p> <ul> <li><code>source_root</code> must exist</li> <li><code>package_root</code> must exist</li> <li><code>__init__.x</code> must exist</li> <li>app projects must contain <code>main.x</code></li> <li>lib projects must not contain <code>main.x</code></li> </ul> <p>Package names must match <code>^[A-Za-z_][A-Za-z0-9_]*$</code>. If <code>project.name</code> is not a valid package identifier, set <code>[build].package</code> explicitly instead of relying on normalization.</p>"},{"location":"manifest/#examples","title":"Examples","text":"<p>Library:</p> <pre><code>[project]\nname = \"astx\"\nversion = \"0.1.0\"\n\n[build]\nmode = \"lib\"\n</code></pre> <p>App:</p> <pre><code>[project]\nname = \"myapp\"\nversion = \"0.1.0\"\n\n[build]\nmode = \"app\"\n</code></pre> <p>Package override:</p> <pre><code>[project]\nname = \"my-project\"\nversion = \"0.1.0\"\n\n[build]\npackage = \"my_project\"\nmode = \"lib\"\n</code></pre>"},{"location":"manifest/#runtime-dependencies","title":"Runtime dependencies","text":"<p>Runtime dependencies live in the <code>project.dependencies</code> array and use PEP 508-style strings. <code>arxpm</code> supports three shapes:</p> <ol> <li>Registry:</li> </ol> <pre><code>dependencies = [\n  \"pyyaml\",\n]\n</code></pre> <ol> <li>Local path:</li> </ol> <pre><code>dependencies = [\n  \"mylib @ ../mylib\",\n]\n</code></pre> <ol> <li>Git:</li> </ol> <pre><code>dependencies = [\n  \"utils @ git+https://example.com/utils.git\",\n]\n</code></pre>"},{"location":"manifest/#dependency-groups","title":"Dependency groups","text":"<p>Non-runtime workflow dependencies such as test, lint, and docs tools live in the top-level <code>[dependency-groups]</code> table.</p> <pre><code>[dependency-groups]\ndev = [\n  \"pytest\",\n  \"ruff\",\n]\n</code></pre>"},{"location":"manifest/#environment","title":"Environment","text":"<p>The <code>[environment]</code> table controls how <code>arxpm install</code> obtains a Python environment for the project. It is optional \u2014 when absent, <code>arxpm</code> behaves as if <code>kind = \"venv\"</code> with <code>path = \".venv\"</code> had been declared.</p>"},{"location":"multi-file-projects/","title":"Multi-file Projects","text":"<p>An Arx project can be composed of several <code>.x</code> files that refer to each other via <code>import</code> statements. <code>arxpm</code> now resolves the package root and passes an explicit file path to <code>arx</code>.</p>"},{"location":"multi-file-projects/#layout","title":"Layout","text":"<pre><code>multi-module/\n\u251c\u2500\u2500 .arxproject.toml\n\u2514\u2500\u2500 src/\n    \u2514\u2500\u2500 multi_module/\n        \u251c\u2500\u2500 __init__.x\n        \u251c\u2500\u2500 main.x\n        \u251c\u2500\u2500 math_utils.x\n        \u2514\u2500\u2500 string_utils.x\n</code></pre> <p><code>.arxproject.toml</code>:</p> <pre><code>[project]\nname = \"multi-module\"\nversion = \"0.1.0\"\nedition = \"2026\"\n\n[build]\npackage = \"multi_module\"\nmode = \"app\"\nout_dir = \"build\"\n\n[toolchain]\ncompiler = \"arx\"\nlinker = \"clang\"\n</code></pre> <p><code>arxpm build</code> invokes the compiler as:</p> <pre><code>arx src/multi_module/main.x --output-file build/multi_module\n</code></pre> <p>Sibling imports still work from inside the package root.</p>"},{"location":"multi-file-projects/#calling-imported-functions","title":"Calling imported functions","text":"<p><code>src/multi_module/math_utils.x</code>:</p> <pre><code>fn add(a: i32, b: i32) -&gt; i32:\n  return a + b;\n</code></pre> <p><code>src/multi_module/string_utils.x</code>:</p> <pre><code>fn greet(name: string) -&gt; string:\n  return \"Hello, \" + name + \"!\";\n</code></pre> <p><code>src/multi_module/main.x</code>:</p> <pre><code>import add from math_utils\nimport greet from string_utils\n\nfn main() -&gt; i32:\n  print(greet(\"Arx\"));\n  print(add(2, 3));\n  return 0;\n</code></pre>"},{"location":"multi-file-projects/#packaging","title":"Packaging","text":"<p><code>arxpm pack</code> and <code>arxpm publish</code> bundle every <code>*.x</code> / <code>*.arx</code> file under the resolved source root, preserving the package-relative layout.</p>"}]}